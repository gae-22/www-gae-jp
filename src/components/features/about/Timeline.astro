---
export interface Props {
    events: {
        year: string;
        title: string;
        company: string;
        description?: string;
    }[];
}

const { events } = Astro.props;
---

<div class="relative border-l border-zinc-200 ml-4 space-y-12 py-4">
  {events.map((event) => (
    <div class="relative pl-8 group">
      <!-- Dot -->
      <div class="absolute -left-1.25 top-2 w-2.5 h-2.5 rounded-full bg-deep border border-zinc-200 group-hover:bg-neon-violet group-hover:border-neon-violet group-hover:shadow-[0_0_10px_rgba(124,58,237,0.5)] transition-all duration-300"></div>

      <div class="flex flex-col sm:flex-row sm:items-baseline gap-2 mb-2">
        <span class="text-neon-cyan font-mono text-sm">{event.year}</span>
        <h3 class="text-xl font-display font-bold">{event.title}</h3>
        <span class="text-text-muted text-sm">@ {event.company}</span>
      </div>

      {event.description && (
        <p class="text-zinc-600 text-sm leading-relaxed max-w-xl">
            {event.description}
        </p>
      )}
    </div>
  ))}
</div>
